import React from 'react';
import { render } from 'react-dom'; // lets us render into our html
import App from 'components/app';

render(<App />, document.getElementById('app'));

//2 days since your last commit! 
